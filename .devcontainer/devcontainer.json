{
  "build": { "dockerfile": "Dockerfile" },

  "postCreateCommand": "npm install -g @angular/cli && cd /workspaces/awd-project-server/mobile-post-office && npm install && ng serve --open",

  "customizations": {
    "vscode": {
      "extensions": ["dbaeumer.vscode-eslint"]
    }
  },

  "forwardPorts": [3000]
}
